diff --git a/assets/panda_tray/panda_tray_cylinder.xml b/assets/panda_tray/panda_tray_cylinder.xml
index 16c2e13..5535baa 100644
--- a/assets/panda_tray/panda_tray_cylinder.xml
+++ b/assets/panda_tray/panda_tray_cylinder.xml
@@ -113,7 +113,7 @@
       </body>
     </body>
 
-    <body name="cylinder" pos="0.788 0.108 0.658"> <!-- 0.621 + 0.03 + 0.005 + 0.002 (offset)-->
+    <body name="cylinder" pos="0.788 0.108 0.655"> <!-- 0.621 + 0.03 + 0.005 + 0.002 (offset)-->
       <freejoint name="cylinder_free"/>
       <geom name="cylinder_geom" type="cylinder" size="0.02 0.03"
             mass="0.2" friction="0.6 0.005 0.0001" material="ball_red"/>
diff --git a/environment.yml b/environment.yml
index 81ffe3b..fd187f4 100644
--- a/environment.yml
+++ b/environment.yml
@@ -52,6 +52,7 @@ dependencies:
 
   # Misc utils
   - cloudpickle
+
   - pip:
       - gymnasium==1.1.1
       - shimmy==2.0.0
@@ -63,4 +64,5 @@ dependencies:
       - rich
       - hydra-core==1.3.2
       - omegaconf==2.3.0
-      - tensorboard
\ No newline at end of file
+      - tensorboard
+      - wandb
\ No newline at end of file
diff --git a/envs/traypose/__pycache__/traypose_env.cpython-39.pyc b/envs/traypose/__pycache__/traypose_env.cpython-39.pyc
index f6a9f97..2610f93 100644
Binary files a/envs/traypose/__pycache__/traypose_env.cpython-39.pyc and b/envs/traypose/__pycache__/traypose_env.cpython-39.pyc differ
diff --git a/envs/traypose/config.yaml b/envs/traypose/config.yaml
index 0e2f71f..3a94b85 100644
--- a/envs/traypose/config.yaml
+++ b/envs/traypose/config.yaml
@@ -17,7 +17,7 @@ start:
   tray_pos: [0.788, 0.108, 0.621]
   tray_rpy: [0.0, 0.0, 2.111]
   joints:   [0.41, 1.16, -0.79, -0.11, -0.73, 1.77, 0.42]
-  cylinder: [0.788, 0.108, 0.658]
+  cylinder: [0.788, 0.108, 0.655]
 
 goal:
   tray_pos: [0.65, 0.50, 0.80]
@@ -25,7 +25,7 @@ goal:
   pos_tolerance: 0.03
   yaw_tolerance_deg: 5.0
   success_hold_steps: 5
-  max_steps: 150
+  max_steps: 500
 
 pd_gains:
   Kq: [2000.0, 2000.0, 2000.0, 1500.0, 500.0, 400.0, 300.0]
diff --git a/envs/traypose/traypose_env.py b/envs/traypose/traypose_env.py
index a5c0a70..799f73b 100644
--- a/envs/traypose/traypose_env.py
+++ b/envs/traypose/traypose_env.py
@@ -107,7 +107,7 @@ class TrayPoseEnv(gym.Env):
         self.goal_pos_tolerance = float(get("goal.pos_tolerance", 0.03))
         self.goal_yaw_tolerance = float(get("goal.yaw_tolerance_deg", 5.0)) * np.pi / 180.0
         self.success_hold_H = int(get("goal.success_hold_steps", 5))
-        self.max_steps = int(get("goal.max_steps", 150))
+        self.max_steps = int(get("goal.max_steps", 500))
 
         # Cylinder start
         self.start_cylinder = np.array(get("start.cylinder", [0.788, 0.108, 0.655]), dtype=np.float64)
@@ -406,14 +406,6 @@ class TrayPoseEnv(gym.Env):
 
         mujoco.mj_forward(self.model, self.data)
 
-        # # --- Add settling phase: run simulation forward with zero action (hold start pose) ---
-        # settle_steps = 100  
-        # self.q_des = self.start_joint_positions.copy()  
-        # for _ in range(settle_steps):
-        #     tau = self._compute_torque_control()  
-        #     self._set_arm_ctrl(tau)               
-        #     mujoco.mj_step(self.model, self.data)
-
         # Record tray pose (ground truth for control)
         self.tray_pos = self.data.xpos[self.tray_body_id].copy()
         tray_quat = self.data.xquat[self.tray_body_id].copy()  # [w,x,y,z]
diff --git a/scripts/torquesensor/visualize_traypose.py b/scripts/torquesensor/visualize_traypose.py
index 933decd..c9766b8 100644
--- a/scripts/torquesensor/visualize_traypose.py
+++ b/scripts/torquesensor/visualize_traypose.py
@@ -68,7 +68,7 @@ def print_observation_details(obs, step=0):
     print(f"Cylinder XY (tray):  {obs[30:32]}")
     print(f"Cylinder vel XY:     {obs[32:34]}")
 
-def run_zero_action_mode(env, viewer, episode, pause_seconds=3.0):
+def run_zero_action_mode(env, viewer, episode, pause_seconds=10.0):
     """Mode 1: Zero action - arm stays at start pose"""
     print(f"\n=== Episode {episode+1} - ZERO ACTION MODE ===")
     
@@ -80,6 +80,7 @@ def run_zero_action_mode(env, viewer, episode, pause_seconds=3.0):
     
     print(f"\nHolding start pose with zero actions for {pause_seconds}s...")
     n_steps = int(pause_seconds / env.control_dt)
+    print(f"\ncontrol dt {env.control_dt}")
     
     action = np.zeros(7, dtype=np.float32)
     print(f"Action (zero): {action}")
@@ -103,8 +104,8 @@ def run_zero_action_mode(env, viewer, episode, pause_seconds=3.0):
             print(f"    Terminated: {terminated}, Truncated: {truncated}, HoldCounter: {info.get('goal_hold_counter')}")
         
         viewer.sync()
-        # time.sleep(env.control_dt)
-        time.sleep(1)
+        time.sleep(env.control_dt)
+        # time.sleep(1)
         
         if done or not viewer.is_running():
             if done:
@@ -269,7 +270,7 @@ def main():
         
         for episode in range(num_episodes):
             if mode == "zero":
-                run_zero_action_mode(env, viewer, episode, pause_seconds=3.0)
+                run_zero_action_mode(env, viewer, episode, pause_seconds=10.0)
             elif mode == "seeded":
                 run_seeded_action_mode(env, viewer, episode, max_steps=500, seed=123)
             else:  # random
diff --git a/scripts/traypose/test_trainpose.py b/scripts/traypose/test_trainpose.py
index 8457c53..217d8c7 100644
--- a/scripts/traypose/test_trainpose.py
+++ b/scripts/traypose/test_trainpose.py
@@ -59,7 +59,8 @@ def evaluate_model(model_path, num_episodes=3):
                     print(f"  Step {step_count}, Reward: {reward:.3f}, Terminated={terminated}, Truncated={truncated}")
                 
                 step_count += 1
-                time.sleep(env.control_dt)  # Maintain real-time speed
+                # time.sleep(env.control_dt)  # Maintain real-time speed
+                time.sleep(1.0)
             
             print(f"Episode {ep+1} finished after {step_count} steps with reward: {ep_reward:.3f}")
             all_rewards.append(ep_reward)
@@ -68,6 +69,6 @@ def evaluate_model(model_path, num_episodes=3):
     print(f"\nAverage reward over {num_episodes} episodes: {avg_reward:.3f}")
 
 if __name__ == "__main__":
-    # model_path = "training/checkpoints/best_model.zip"
-    model_path = "checkpoints/best.pt"
+    model_path = "training/checkpoints/best_model.zip"
+    # model_path = "checkpoints/best.pt"
     evaluate_model(model_path)
\ No newline at end of file
diff --git a/scripts/traypose/train_traypose.py b/scripts/traypose/train_traypose.py
index 571e93a..6e814d9 100644
--- a/scripts/traypose/train_traypose.py
+++ b/scripts/traypose/train_traypose.py
@@ -4,6 +4,7 @@ import sys
 import time
 import numpy as np
 import gymnasium as gym
+import wandb
 from stable_baselines3 import PPO
 from stable_baselines3.common.callbacks import EvalCallback, CheckpointCallback, BaseCallback
 from stable_baselines3.common.env_util import make_vec_env
@@ -19,12 +20,47 @@ if PROJECT_ROOT not in sys.path:
 # Import custom env
 from envs.traypose.traypose_env import TrayPoseEnv
 
+class WandbCallback(BaseCallback):
+    def __init__(self, verbose=0):
+        super().__init__(verbose)
+
+    def _on_step(self) -> bool:
+        # Log training metrics to wandb
+        if self.n_calls % 100 == 0:  # log every 100 steps
+            wandb.log({
+                "train/episode_reward": self.locals.get("rewards", [0])[-1] if "rewards" in self.locals else 0,
+                "train/step": self.num_timesteps,
+            })
+        return True
+
+class ZeroActionFirstStepsWrapper(gym.Wrapper):
+    def __init__(self, env, zero_steps=10):
+        super().__init__(env)
+        self.zero_steps = zero_steps
+        self.current_step = 0
+
+    def reset(self, **kwargs):
+        self.current_step = 0
+        return self.env.reset(**kwargs)
+
+    def step(self, action):
+        if self.current_step < self.zero_steps:
+            zero_action = np.zeros_like(action)
+            obs, reward, terminated, truncated, info = self.env.step(zero_action)
+            self.current_step += 1
+            return obs, reward, terminated, truncated, info
+        else:
+            obs, reward, terminated, truncated, info = self.env.step(action)
+            self.current_step += 1
+            return obs, reward, terminated, truncated, info
+
 
 def make_env_fn(model_path="assets/panda_tray/panda_tray_cylinder.xml",
                 config_path="envs/traypose/config.yaml",
                 obs_noise_std_pos=0.0,
                 obs_noise_std_vel=0.0,
-                use_jacobian_tray_obs=False):
+                use_jacobian_tray_obs=False,
+                zero_steps=10):  # Added zero_steps parameter
     """
     Factory returning a function that creates a fresh env instance wrapped with Monitor.
     """
@@ -36,6 +72,7 @@ def make_env_fn(model_path="assets/panda_tray/panda_tray_cylinder.xml",
             obs_noise_std_vel=obs_noise_std_vel,
             use_jacobian_tray_obs=use_jacobian_tray_obs,
         )
+        env = ZeroActionFirstStepsWrapper(env, zero_steps=zero_steps)  # Wrap with zero action wrapper
         env = Monitor(env)  # Gymnasium-compatible Monitor
         return env
     return _thunk
@@ -177,13 +214,37 @@ class CustomEvalCallback(BaseCallback):
 
 
 def main():
+    # Initialize Weights & Biases
+    import wandb
+    wandb.init(
+        project="franka_tray_rl",
+        sync_tensorboard=True,
+        config={
+            "n_envs": 8,
+            "n_steps": 1024,
+            "batch_size": 256,
+            "total_timesteps": 1_000_000,
+            "learning_rate": 1e-4,
+            "gamma": 0.99,
+            "gae_lambda": 0.95,
+            "clip_range": 0.2,
+            "ent_coef": 1e-3,
+            "vf_coef": 0.5,
+            "n_epochs": 10,
+            "seed": 42,
+        },
+        name="ppo_traypose_run",
+        save_code=True,
+    )
+
     # 1) Vectorized envs
     make_env = make_env_fn(
         model_path="assets/panda_tray/panda_tray_cylinder.xml",
         config_path="envs/traypose/config.yaml",
         obs_noise_std_pos=0.0,
         obs_noise_std_vel=0.0,
-        use_jacobian_tray_obs=False
+        use_jacobian_tray_obs=False,
+        zero_steps=10,  # Pass zero_steps here
     )
 
     # Directories
@@ -233,7 +294,7 @@ def main():
     except Exception as e:
         print("Warning: could not share obs_rms between train/eval VecNormalize (shape mismatch?). Continuing without sharing. Error:", e)
 
-    # 4) Callbacks: keep EvalCallback + custom success-rate callback
+    # 4) Callbacks: keep EvalCallback + custom success-rate callback + WandbCallback
     eval_callback = EvalCallback(
         eval_env,
         best_model_save_path=save_dir,
@@ -250,7 +311,7 @@ def main():
         eval_freq=5_000,
         best_model_save_path=save_dir,
         verbose=1,
-        success_hold_H=None,  # None => try to infer from env, or set integer explicitly
+        success_hold_H=None,
     )
 
     checkpoint_callback = CheckpointCallback(
@@ -261,9 +322,17 @@ def main():
         save_vecnormalize=True,
     )
 
+    wandb_callback = WandbCallback(
+        model_save_path=os.path.join(save_dir, "wandb_models"),
+        verbose=2,
+    )
+
     # 5) Train
     try:
-        model.learn(total_timesteps=total_timesteps, callback=[eval_callback, custom_eval_cb, checkpoint_callback])
+        model.learn(
+            total_timesteps=total_timesteps,
+            callback=[eval_callback, custom_eval_cb, checkpoint_callback, wandb_callback]
+        )
         model.save(os.path.join(save_dir, "ppo_traypose_final"))
         train_env.save(os.path.join(save_dir, "vecnormalize.pkl"))
     finally:
diff --git a/scripts/traypose/visualize_traypose.py b/scripts/traypose/visualize_traypose.py
index 13bacc7..e2ad6a3 100644
--- a/scripts/traypose/visualize_traypose.py
+++ b/scripts/traypose/visualize_traypose.py
@@ -283,7 +283,7 @@ def main():
         
         for episode in range(num_episodes):
             if mode == "zero":
-                run_zero_action_mode(env, viewer, episode, pause_seconds=3.0)
+                run_zero_action_mode(env, viewer, episode, pause_seconds=10.0)
             elif mode == "seeded":
                 run_seeded_action_mode(env, viewer, episode, max_steps=500, seed=123)
             else:  # random
